import requests, json

from main_config import token, main_url, head
from string_helper import randomString

# def test_create_product():
#     res = requests.post(main_url+ "inventory/createProduct", headers=head)
#     print(res)
#     assert res == True
